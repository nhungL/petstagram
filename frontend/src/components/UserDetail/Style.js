import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  title: {
    fontWeight: "bold",
  },
  background: {
    marginTop: "5vw",
    marginLeft: "5vw",
  },
}));
export default useStyles;
