import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  background: {
    margin: "50px",
  },
  title: {
    fontWeight: "bold",
    marginBottom: "25px",
  },
  listLayout: {
    height: "500",
    width: "auto",
  },
}));
export default useStyles;
