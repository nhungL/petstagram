import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  uploadimagebutton: {
    marginLeft: "50px",
    color: "#C4C4C4",
  },
  uploadvideobutton: {
    marginLeft: "10px",
    color: "#C4C4C4",
  }
}));
export default useStyles;
