import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  search: {
    width: "500px",
    marginTop: "50px",
  },
  pic: {
    width: "80px",
    marginTop: "30px",
    marginRight: "30px",
  },
  button: {
    marginTop: "50px",
  },
}));
export default useStyles;
